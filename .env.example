VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Public URL of the deployed app. Only used to build the link in Supabase's confirmation and
# recovery emails — set it wherever the current origin isn't a usable link (a dev server, and the
# packaged mobile app). Leave empty on a plain web deploy.
VITE_PUBLIC_APP_URL=
